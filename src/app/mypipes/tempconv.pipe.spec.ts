import { TempconvPipe } from './tempconv.pipe';

describe('TempconvPipe', () => {
  it('create an instance', () => {
    const pipe = new TempconvPipe();
    expect(pipe).toBeTruthy();
  });
});
